{
  "actorSpecification": 1,
  "fields": {
    "type": "object",
    "properties": {
      "transaction_id": {
        "type": "string",
        "description": "UUID v4 identifier for the transaction"
      },
      "goal_id": {
        "type": "string",
        "description": "UUID of the savings goal this transaction belongs to"
      },
      "user_id": {
        "type": "string",
        "description": "UUID of the user who owns the goal"
      },
      "date": {
        "type": "string",
        "description": "Date of the transaction (YYYY-MM-DD)"
      },
      "transaction_type": {
        "type": "string",
        "description": "deposit or withdrawal"
      },
      "amount": {
        "type": "number",
        "description": "Transaction amount in local currency units (always > 0)"
      }
    },
    "required": ["transaction_id", "goal_id", "user_id", "date", "transaction_type", "amount"]
  },
  "views": {
    "transactions": {
      "title": "Transactions",
      "transformation": {
        "fields": ["date", "transaction_type", "amount", "goal_id", "user_id", "transaction_id"]
      },
      "display": {
        "component": "table",
        "properties": {
          "date": { "label": "Fecha", "format": "date" },
          "transaction_type": { "label": "Tipo", "format": "text" },
          "amount": { "label": "Monto", "format": "number" },
          "goal_id": { "label": "goal_id", "format": "text" },
          "user_id": { "label": "user_id", "format": "text" },
          "transaction_id": { "label": "transaction_id", "format": "text" }
        }
      }
    }
  }
}
